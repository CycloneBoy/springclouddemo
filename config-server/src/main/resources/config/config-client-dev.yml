server:
  port: 9004

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/   # 注册中心地址
#  instance:
#    prefer-ip-address: true # 使用ip地址注册到eureka server
#    instance-id: ${spring.cloud.client.ipAddress}:${server.port} # eureka server中看到的status显示为具体的ip地址和port

from: default-test123 456789
config:
  demo: test-config 123
